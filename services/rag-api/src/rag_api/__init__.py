"""
DataMind RAG API — 4-Tier Agent Memory, GraphRAG, MMR Retrieval, RAGAS Evaluation.
Day 22: Phase 5 — RAG & Reporting backbone.

Protocols: None (REST API)
SOLID: DIP (all stores injected via lifespan), SRP (each module owns one concern)
Benchmark: tests/benchmarks/bench_rag.py
"""

__version__ = "0.1.0"
