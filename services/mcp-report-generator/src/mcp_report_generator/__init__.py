"""
DataMind MCP Report Generator — WeasyPrint PDF + PPTX + Merkle provenance.
Day 11: Phase 2 — MCP Tool Server for report generation and IPFS anchoring.

Protocols: MCP (JSON-RPC 2.0, streamable-HTTP, tools/list + tools/call)
SOLID: SRP (each module owns one concern), OCP (new formats = new file implementing ABC)
"""
