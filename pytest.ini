[pytest]
asyncio_mode = auto
markers =
    integration: marks tests that require a running docker compose stack (deselect with '-m "not integration"')
    slow: marks tests that take > 10s
testpaths = tests
timeout = 60
